package controller;

/**
 * EventConnector is responsible for connecting user choices with our
 * application classes.  The only code that should be added here is
 * additional calls to addEvent(), when new functionality is added.
 */
public interface EventConnector {
    void setup();
}
